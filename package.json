{
  "name": "laundry-shop-system",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd client && pnpm dev",
    "build": "cd client && pnpm build",
    "lint": "cd client && pnpm lint",
    "preview": "cd client && pnpm preview"
  },
  "workspaces": [
    "client"
  ]
}
